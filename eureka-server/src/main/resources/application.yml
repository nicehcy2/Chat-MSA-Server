spring:
  application:
    name: Eureka Server

server:
  port: 8070

eureka:
  instance:
    hostname: localhost # (?? ?????) eureka-server
  client:
    register-with-eureka: false # Eureka ?? ??? Eureka? ???? ??. ?? ?? ???? ???? ??
    fetch-registry: false # ?? ??? ?????? ???? ??. ?? ?? ???? ???? ??.
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/