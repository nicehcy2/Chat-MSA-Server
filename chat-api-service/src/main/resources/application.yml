spring:
  config:
    import: "optional:configserver:http://${CONFIG_HOST:configserver}:${CONFIG_PORT:7071}"
  application:
    name: chat-api-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:docker}