spring:
  config:
    import: "optional:configserver:http://${CONFIG_HOST:localhost}:${CONFIG_PORT:7071}"
  application:
    name: chat-api-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}