server:
  port: 8072

spring:
  cloud:
    gateway:
      server:
        webflux:
          discovery.locator:
            enabled: true
            lowerCaseServiceId: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,gateway,gatewaymvc

eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8070/eureka