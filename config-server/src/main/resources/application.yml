spring:
  application:
    name: config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/nicehcy2/Chat-MSA-Server.git"
          search-paths: "config-server/src/main/resources/config"

server:
  port: 7071